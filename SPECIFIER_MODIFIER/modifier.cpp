// if we not use any access specifier, the default access specifier is private 
// in the case of member function we can not use private access specifier and protected access specifier 
// we only use public access specifier for member function because we want to access them outside the class