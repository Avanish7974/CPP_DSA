// time complexity of the given code is O(1) because the program only performs a fixed number
//  of operations regardless of the size of the input. In this case, it simply assigns the value 10 to
#include <iostream>
using namespace std;
int main() {
    int x;
    x = 10;
    cout << "The value of x is: " << x << endl;
    // The program only performs a fixed number of operations regardless of the size of the input.
    
    return 0;
}
// Time Complexity: O(1)
// Space Complexity: O(1)
// The space complexity is also O(1) because the program uses a fixed amount of memory to store the variable x.
// wo koi sa bhi operation jisme hum na koi loop use karein na koi recursion use karein
// aur na koi function call karein to uska time complexity hamesha O(1) hi hota hai.
