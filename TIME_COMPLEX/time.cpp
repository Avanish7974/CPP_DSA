#include <iostream>
using namespace std;
int main() {
    int x;
    for(x = 1; x <= 10; x++) {
        cout << "The value of x is: " << x << endl;

    }
    // The program only performs a fixed number of operations regardless of the size of the input.
    
    return 0;
}
// time complexity of the given code is O(n) because the program performs a linear number of operations
// sapce complexity: O(1) because the program does not depend on the size of the input.
// #include <iostream>
// using namespace std;
// int main() {
//     int x;
//     for(x = 1; x <= 10; x++) {
//         if(x % 2 == 0) {
//             cout << "The value of x is: " << x << endl;
//         }
        
//     }
    
//     return 0;
// }
// time complexity of the given code is O(n) because the program performs a linear number of operations
// sapce complexity: O(1) because the program does not depend on the size of the input.